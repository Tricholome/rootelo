name: Deploy Frog Test to DEV Mirror

on:
  push:
    branches:
      - frog
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Cache Pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Build Frog Page
        env:
          API_TOKEN: ${{ secrets.API_TOKEN }}
          ROOTDB_TOKEN: ${{ secrets.ROOTDB_TOKEN }}
        run: |
          python frog.py
          mkdir -p build_site
          cp frog.html build_site/
          cp -r assets build_site/

      - name: Deploy to rootelo-dev repository
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.DEV_REPO_PAT }}
          external_repository: tricholome/rootelo-dev
          publish_dir: ./build_site
          publish_branch: gh-pages
          keep_files: true
